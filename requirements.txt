# Runtime dependencies for the FastAPI backend (used by the Dockerfile and the
# Render blueprint). Local dev installs this plus backend/requirements-dev.txt.
fastapi>=0.111,<0.116
uvicorn[standard]>=0.30
SQLAlchemy[asyncio]>=2.0.30
alembic>=1.13
asyncpg>=0.29
pydantic>=2.7
pydantic-settings>=2.3
httpx>=0.27
openai>=1.40
workos>=5,<6
trafilatura>=1.12
